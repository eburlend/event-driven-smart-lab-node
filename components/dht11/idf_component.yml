# This file is not a build script
# It is a dependency manifest
# Comparable to:
# - Node.js (package.json)
# - .NET (csproj)
# - Python (requirements.txt)
dependency: 

# Namespaces (esp-idf-lib) + component name (dht)
# "*" -- means latest compatible version
# for production "^1.0.0"
# ^ means any newer version that is compatible
esp-idf-lib/dht: "*"
